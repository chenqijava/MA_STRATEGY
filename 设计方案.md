# 趋势 + 均值回归结合策略 — 设计方案

> 状态：待审阅。审阅通过后再进入编码。
> 标的：ETH-USDT-SWAP　周期：5m / 15m 双周期对比　资金：沿用 backtest_core（1万U，taker 0.05%，滑点 0.02%）

---

## 1. 核心设计原则

趋势与均值回归是**互斥**的两套逻辑，不能在同一根 K 线上简单相加，否则信号互相打架。
本方案采用 **Regime Switching（市场状态切换）+ 多时间框架分工** 的组合架构：

- 一个**状态判别器**先判断当前是「趋势市」还是「震荡市」；
- 趋势市 → 只允许**突破/顺势**开仓（复用现有 backtest_core 逻辑）；
- 震荡市 → 只允许**逆势回归**开仓（本次新增）；
- 任一时刻**至多持有一套仓位**，两腿绝不叠加。

---

## 2. 整体架构

```
                 ┌─────────────────────┐
   K线数据 ──────▶│  指标层 calc_indicators │
                 └──────────┬──────────┘
                            ▼
                 ┌─────────────────────┐
                 │ 状态判别器 regime()   │  ← ADX + 波动率 + 价格位置
                 └──────────┬──────────┘
              趋势市 │                 │ 震荡市
                    ▼                 ▼
        ┌────────────────┐   ┌────────────────────┐
        │ 趋势腿 signal_tr │   │ 回归腿 signal_mr      │
        │ (突破+ADX+1H确认) │   │ (布林带+RSI 双确认)   │
        └────────┬───────┘   └─────────┬──────────┘
                 └──────────┬──────────┘
                            ▼
                 ┌─────────────────────┐
                 │ 单仓状态机 run_backtest│  ← 分腿出场 + 统一风控
                 └─────────────────────┘
```

复用现有 [backtest_core.py](../py/backtest_core.py) 的账户/成本/风控框架，新增回归腿与状态判别，改造出场逻辑使其能区分两腿。

---

## 3. 状态判别器（regime）

| 判别 | 条件 | 说明 |
|------|------|------|
| **趋势市 (TREND)** | `ADX > ADX_TREND`（默认 25）且 `ADX` 上升 | 有方向、有动能 |
| **震荡市 (RANGE)** | `ADX < ADX_RANGE`（默认 20）且 价格处于布林带内 | 无方向、来回摆 |
| **中间态 (NEUTRAL)** | 20 ≤ ADX ≤ 25 | **不开新仓**，避免状态边界处的假信号 |

设两条阈值线（25 / 20）留出缓冲带，防止 ADX 在单一阈值附近抖动导致策略在两套逻辑间反复横跳。

---

## 4. 趋势腿（TREND leg）— 复用现有逻辑

沿用 backtest_core 现有突破逻辑，仅把开仓门槛挂到「趋势市」状态下：

- 入场：`收盘突破 N 周期通道 & 放量 & ADX 达标 & 收盘在 EMA 同侧 & 1H 趋势同向`
- 出场：**ATR 移动止损 + ATR 倍数目标止盈 + EMA 反向止损 + 时间止损**（现有逻辑不动）
- 特点：让利润奔跑，止损给足空间（宽止损、大止盈）

---

## 5. 回归腿（MEAN-REVERSION leg）— 本次新增

### 5.1 入场信号（主方案：布林带 + RSI 双确认）

- 布林带：`mid = SMA(close, BB_LEN=20)`，`upper/lower = mid ± BB_MULT(=2.0)·std`
- RSI：`RSI_LEN=14`
- **做多（抄底）**：`low ≤ lower 下轨` 且 `RSI < RSI_OS(=30)`，且处于震荡市
- **做空（摸顶）**：`high ≥ upper 上轨` 且 `RSI > RSI_OB(=70)`，且处于震荡市

### 5.2 对照方案（回测时并行对比，择优保留）

- **z-score 偏离**：`z = (close − SMA) / rolling_std`，`z < −Z_TH(=2)` 做多、`z > +Z_TH` 做空。
- 复用 [s1.py](../py/Strategy/s1.py) 已有的 `ts_mean / ts_std` 算子，实现成本极低。
- 回测阶段用同一份 ETH 数据两版对跑，按夏普 / 最大回撤 / 胜率决定哪版进入最终策略。

### 5.3 出场逻辑（**与趋势腿完全不同 — 关键**）

回归交易必须**快进快出、小止盈、硬止损**：

| 出场 | 规则 |
|------|------|
| **止盈（主）** | 价格回到布林带中轨（均值回归目标达成）即平仓 |
| **硬止损（关键）** | 价格反向突破区间边界 `MR_SL_ATR(=1.0)·ATR` 立即认亏 —— 回归失败往往就是趋势启动，这是最危险的亏损来源，止损必须紧 |
| **时间止损** | 持仓超过 `MR_MAX_BARS(=12)` 根仍未回归 → 平仓（回归不该拖太久） |
| **状态止损** | 若持仓期间市场从震荡市转为趋势市 → 立即平回归单（前提假设已破坏） |

---

## 6. 统一风控（沿用 + 增强）

- **仓位互斥**：`position != 0` 时不开任何新仓，两腿共用一个仓位槽。
- 单笔风险 `RISK_PER_TRADE`、名义敞口上限 `MAX_NOTIONAL_PCT`、单日最大亏损 `MAX_DAILY_LOSS` —— 全部沿用现有实现。
- 回归腿单独用更紧的止损倍数（`MR_SL_ATR` 独立于趋势腿的 `ATR_SL_MULT`）。
- 手续费 / 滑点：沿用 backtest_core（进出各扣一次单边）。

---

## 7. 参数清单（新增部分）

```python
REGIME = dict(ADX_TREND=25, ADX_RANGE=20)
MR = dict(
    BB_LEN=20, BB_MULT=2.0,
    RSI_LEN=14, RSI_OS=30, RSI_OB=70,
    Z_TH=2.0,                    # 对照方案用
    MR_SL_ATR=1.0,              # 回归腿硬止损（ATR 倍数）
    MR_MAX_BARS=12,             # 回归腿时间止损
    MR_SIGNAL='bbands',         # 'bbands' | 'zscore' 切换主/对照
)
```

趋势腿参数继续用 backtest_core 的 `DEFAULTS`，不改动。

---

## 8. 回测与对比计划

1. **基线**：单独跑趋势腿、单独跑回归腿，各自的绩效作为对照基准。
2. **结合**：跑 Regime 切换后的完整策略。
3. **周期对比**：5m 与 15m 各跑一遍，共 2×（基线+结合）组合。
4. **信号对比**：回归腿 `bbands` vs `zscore` 各跑一遍。
5. **评估指标**：年化收益、夏普、最大回撤、胜率、盈亏比、交易次数、两腿各自贡献。
6. **验收标准**：结合策略的**夏普 / 回撤应优于任一单腿**，否则说明「结合」没有正贡献，回到设计。

---

## 9. 主要风险点（诚实提醒）

- **状态判别滞后**：ADX 是滞后指标，趋势启动初期可能仍被判为震荡市，导致回归腿逆势挨打 → 靠 5.3 的「状态止损 + 硬止损」兜底。
- **过拟合**：参数一多就容易在历史数据上跑出好看曲线 → 坚持样本外验证（你有 2025 与 2026 两段数据，可做时间切分）。
- **加密市场趋势性强**：ETH 长期趋势成分偏重，回归腿可能整体贡献有限 → 回测数据说话，若回归腿常年拖后腿，就退化为「纯趋势 + 震荡市空仓」。

---

## 10. 文件规划（编码阶段）

```
趋势+均值回归结合策略/
├── 设计方案.md              # 本文档
├── combo_core.py           # 结合策略回测核心（基于 backtest_core 扩展）
├── run_compare.py          # 5m/15m × 基线/结合 × bbands/zscore 批量对比
└── results/                # 回测结果与绩效表
```

---

**下一步**：请审阅本方案。确认后我从 `combo_core.py` 开始实现，先跑通基线与结合的对比回测。
若某处（阈值、出场规则、周期优先级）想调整，直接告诉我。
